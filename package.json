{
  "name": "timetrack",
  "version": "0.1.0",
  "description": "Local-first macOS time tracker: watcher daemon + SQLite + CLI reports",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "report": "bun run src/report.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "get-windows": "^9.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
